Bài 48: Xem kết quả thi
Khi user submit cuối cùng thì show điểm ra
*** Sửa Domain
ExamResultAggregate
- Sửa AnswerResult .. 
 public AnswerResult(string id, string content, bool? userChosen, bool isCorrect)
	{
		Id = id;
		UserChosen = userChosen;
		Content = content;
		IsCorrect = isCorrect;
	}
	
- Đơn giản khi chúng ta submit xong chỉ cần so sánh isCorrect thôi: Nhưng mà isCorrect ko phải so sánh vì finish exam chúng ta đã tính toán pass, fail rồi, nghiệp vụ tính hết rồi

- Chọn template html: student-quiz-result-detail

- Trang PortalApp> Pages > Exams > ExamResult
- Sửa hàm OnGet, OnPost